mixin orders(order)

  .bookings
    ul
     li= `Order ref: ${order._id}`
     each data in order.hotel_data
      li=`Hotel: ${data.hotel_name}` 
      li=`Region: ${data.region}` 
      li=`Departure: ${order.order_details.dateOfDeparture}` 
      li=`Nights: ${order.order_details.duration}` 
      li=`Guests: ${order.order_details.numberOfGuests}` 
      
    
    //- table(border="2" class="table")
    //-    tr
    //-      th ORDER REF
    //-      th HOTEL
    //-      th REGION
    //-      th DEPARTURE
    //-      th NIGHTS
    //-      th GUESTS
    //-    tr
    //-    each data in order.hotel_data
    //-      td= `${order._id}`
    //-      td= `${data.hotel_name}`
    //-      td= `${data.region}`
    //-      td= `${order.order_details.dateOfDeparture}` 
    //-      td= `${order.order_details.duration}` 
    //-      td= `${order.order_details.numberOfGuests}`
