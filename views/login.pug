extends layout

block content
  
  h2= title
  hr
  
  form(action="" method="POST" href="#login_here" id="Paul")
    .form_input
       label(for="email") Email:
       input(type="email" name="email" id="email")

    .form_input
       label(for="password") Password:
       input(type="password" name="password" id="password")

    form_input
      button(type="submit" class="button_small") Log in     
