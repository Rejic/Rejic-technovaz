extends layout
include mixins/_hotel

block content
  h2= title
  each i in hotel
    +hotel(i, searchData)

//- i means each info/data in each hotel || also "i"  means individual Hotel || contain searchData like date,    